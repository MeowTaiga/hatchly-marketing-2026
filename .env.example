# New 2026 API (preferred)
VITE_API_URL=http://localhost:5000

# Legacy fallback used when VITE_API_URL is unset:
# https://api.hatchly.me/api/waitlist

# Discord visitor/click embeds (webhook is client-side).
# Set to false locally to avoid pinging Discord during dev.
# Leave unset or true in production (Vercel).
VITE_DISCORD_PING_ENABLED=false
